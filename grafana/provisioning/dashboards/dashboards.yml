apiVersion: 1

providers:
  - name: eventpulse
    folder: EventPulse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
